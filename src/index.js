import React from 'react';
import ReactDOM from 'react-dom';
import WebPage from './components/WebPage';

const root = document.getElementById('root');

ReactDOM.render(React.createElement(WebPage), root);